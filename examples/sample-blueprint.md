# Sample Blueprint — FocusFlow

> Generated by Cauldron OS 2.1 with Impeccable Taste + Cursor Design Reference

---

# Project Blueprint

## PRD

**App Concept:** FocusFlow is a minimalist Pomodoro timer with integrated task sequencing and ambient soundscapes. It helps deep workers maintain flow state through structured intervals and environmental audio.

**Core Features:**
- Timer with 25/5, 50/10, custom intervals
- Task queue with micro-checklist per session
- Ambient mixer: rain, café, white noise, synth drones
- Session history + streaks
- Optional: Slack/Notion integration for task sync

**Target Users:** Remote knowledge workers, students with ADHD, writers, indie developers, creative professionals who need structured focus time.

---

## Database Schema

```json
{
  "tables": [
    {
      "name": "users",
      "fields": {
        "id": "uuid PK",
        "email": "varchar(255) UNIQUE",
        "created_at": "timestamp"
      }
    },
    {
      "name": "tasks",
      "fields": {
        "id": "uuid PK",
        "user_id": "uuid FK → users.id",
        "title": "varchar(255)",
        "description": "text",
        "completed": "boolean DEFAULT false",
        "due_date": "timestamp NULL",
        "created_at": "timestamp"
      }
    },
    {
      "name": "sessions",
      "fields": {
        "id": "uuid PK",
        "user_id": "uuid FK",
        "task_id": "uuid FK NULL",
        "duration_minutes": "integer",
        "completed": "boolean",
        "ended_at": "timestamp NULL",
        "created_at": "timestamp"
      }
    },
    {
      "name": "soundscapes",
      "fields": {
        "id": "uuid PK",
        "name": "varchar(100)",
        "url": "text",
        "volume": "float DEFAULT 0.5"
      }
    }
  ]
}
```

---

## Security Posture

- **Auth**: Email + passwordless magic link (Supabase Auth or Clerk)
- **Rate limiting**: 10 session starts/min per IP
- **Validation**: All timestamps server-validated; duration max 120 min
- **Secrets**: Audio bucket keys kept in .env; never exposed client-side
- **Data protection**: Database encrypted at rest; GDPR-compliant data export/delete

---

## Architecture Notes

**Frontend:**
- Next.js 14 App Router (TypeScript, Tailwind CSS)
- Shadcn/ui components (Cursor-style dark theme)
- Zustand for lightweight state
- Vercel Analytics + Sentry

**Backend / APIs:**
- Next.js route handlers (API routes)
- PostgreSQL via Supabase or Neon
- Server actions for mutations where possible

**Database:**
- Single DB: `focusflow_prod`
- Tables as above
- Indexes: `user_id`, `created_at` for history queries

**Hosting / deployment:**
- Vercel (frontend + serverless functions)
- Supabase (PostgreSQL + Auth + Storage)
- Cloudflare R2 for audio files (low-cost)

**Integrations:**
- Optional: Notion API for task import
- Optional: Slack webhook for "session complete" notifications

---

## Implementation Priority

1. MVP: Timer + task list + one default sound (rain)
2. History view + streaks
3. Sound mixer UI
4. Integrations (Notion/Slack)

---

## Risk Assessment

- Low complexity — well-scoped
- Audio licensing: use only royalty-free sounds initially
- Timezones: store all timestamps UTC, convert client-side

---

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>FocusFlow — Deep Work Timer</title>
  <style>
    /* Preview styles — actual implementation uses Tailwind + shadcn */
    body { font-family: -apple-system, sans-serif; background: #0d0d0f; color: #d9d9dd; padding: 2rem; }
    .timer { font-size: 4rem; font-weight: 600; color: #b794f6; margin: 2rem 0; }
    .task-list { list-style: none; padding: 0; }
    .task { padding: 0.75rem; border: 1px solid rgba(255,255,255,0.1); margin-bottom: 0.5rem; border-radius: 2px; }
    .btn { background: #1a1a1d; color: #b794f6; border: 1px solid rgba(183,148,246,0.3); padding: 0.75rem 1.5rem; cursor: pointer; }
    .btn:hover { border-color: rgba(183,148,246,0.6); transform: translateY(-1px); }
  </style>
</head>
<body>
  <h1>FocusFlow</h1>
  <div class="timer">25:00</div>
  <button class="btn">Start Session</button>
  <ul class="task-list">
    <li class="task">Write Cauldron OS README</li>
    <li class="task">Design DB schema for focus sessions</li>
  </ul>
</body>
</html>
```

---

*End of blueprint.*
